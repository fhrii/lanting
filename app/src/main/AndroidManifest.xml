<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="academy.bangkit.lanting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lanting">
        <activity android:name=".ui.taskimage.TaskImageActivity"></activity>
        <activity android:name=".ui.task.TaskActivity" />
        <activity android:name=".ui.article.ArticleActivity" />
        <activity android:name=".ui.recipe.RecipeActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.profilecategory.ProfileCategoryActivity" />
        <activity android:name=".ui.profiles.ProfilesActivity" />
        <activity android:name=".ui.profilenew.ProfileNewActivity" />
        <activity android:name=".ui.getting.GettingActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>